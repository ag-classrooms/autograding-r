name: Docker Build

on:
  push:
    branches:
      - ag/docker-cache
  workflow_dispatch:

permissions:
  checks: write
  actions: read
  contents: read

concurrency: 
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true  
  
jobs:
  build:
    name: Docker build
    runs-on: ubuntu-20.04
    if:      ${{ !contains(github.actor,'classroom' )}}
    env:
      RENV_PATHS_ROOT: ~/.local/share/renv
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      # - name: Build and push
      #   uses: docker/build-push-action@v3
      #   with:
      #     context: .
      #     file: .tests/Dockerfile
      #     push: false
      #     tags: agarbuno/gh-grader:4.2.1
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
      - name: Grade homework 💻
        run: |
          docker run \
          -v "${GITHUB_WORKSPACE}:/home/rstudio/" \
          agarbuno/gh-grader:4.2.1 Rscript .tests/grade.R
        
